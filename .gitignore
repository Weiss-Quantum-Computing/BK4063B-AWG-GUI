# Saved instrument setups - these belong in the lab notebook, not in git
awg_setups/
*.json
*.txt
*.csv
*.npy

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# ...except the worked example, which is part of the project. Named rather than
# globbed: uploaded waveforms are now cached as CSV too, and those are bench
# artefacts - a megapoint record is a 10 MB file with no business in git. The
# .txt and .npy copies of the example stay out as well, being the same samples
# in another wrapper that the generator script remakes.
!Waveforms/blackman_100kHz_10ms.csv
